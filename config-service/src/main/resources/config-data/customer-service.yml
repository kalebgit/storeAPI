server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true        # registra por IP, no por hostname
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30
